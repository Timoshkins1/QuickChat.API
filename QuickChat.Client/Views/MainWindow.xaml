<Window x:Class="QuickChat.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="QuickChat"
        Width="950" Height="600"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF8C00"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#2C2C2C"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="MessageBrush" Color="#333333"/>
        <SolidColorBrush x:Key="MessageMineBrush" Color="#FF8C00"/>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2E2E2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" Padding="10">
            <StackPanel>
                <Button Content="Создать чат" Click="CreateChat_Click"/>
                <Button Content="Подключиться к чату" Click="JoinChat_Click"/>

                <TextBlock Text="Ваши чаты" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="5,20,5,10"/>
                <ListBox x:Name="ChatsList" DisplayMemberPath="Name" 
                         Background="Transparent"
                         Foreground="White"
                         BorderThickness="0"
                         SelectionChanged="ChatsList_SelectionChanged"/>
            </StackPanel>
        </Border>

        <!-- Chat area -->
        <Grid Grid.Column="1" Background="{StaticResource BackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Messages -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                <ItemsControl x:Name="MessagesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="{Binding Sender}" 
                                           Foreground="#AAAAAA"
                                           FontWeight="Bold"
                                           Margin="0,0,0,2"/>
                                <Border Background="{Binding IsMine, Converter={StaticResource MessageColorConverter}}"
                                        CornerRadius="8"
                                        Padding="10"
                                        Margin="0"
                                        MaxWidth="400">
                                    <TextBlock Text="{Binding Text}"
                                               Foreground="White"
                                               TextWrapping="Wrap"/>
                                </Border>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Input -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
                <TextBox x:Name="MessageBox" Width="600" Margin="0,0,10,0" Height="35" />
                <Button Content="Отправить" Width="100" Click="SendButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>